import { newComplete } from './complete'
import { newRecord } from './record'
import { newTrick } from './trick'
import { newPlayer } from './player'

export { newComplete, newRecord, newTrick, newPlayer }
